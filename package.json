{
  "name": "field-tech-companion",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm --workspace apps/web run dev",
    "dev:api": "npm --workspace apps/api run dev",
    "lint": "npm run -ws lint",
    "typecheck": "npm run -ws typecheck"
  }
}
